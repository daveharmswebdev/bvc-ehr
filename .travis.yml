language: node_js
node_js:
  - "node"
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
services:
  - postgresql
before_script:
  - psql -c 'create database bvctesting;' -U postgres
after_success: 'npm run coveralls'

  